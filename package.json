{
  "name": "server",
  "version": "0.0.1",
  "description": "node js server",
  "main": "server.js",
  "engines": {
    "node": "14.x"
  },
  "dependencies": {
    "express": "^4.18.2",
    "socket.io": ">=2.4.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  }
}
